---
apache.Apache:
  config_files:
  - /etc/httpd/conf/httpd.conf
  ports:
  - 80
  - 443
  - 8080
  - 8443
  requires-host-networking: true
  rpms:
  - httpd
  services:
  - httpd
  targets_services:
  - httpd

